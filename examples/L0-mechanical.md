# L0｜机械任务示例

【任务】
把现有说明中的一个过时命令替换为当前命令，并保持其余文档不变。

【风险】
L0；单点、可回滚的文档修改。

【边界】
- 只修改命令所在的说明文件。
- 保留原有语气、结构和示例上下文。
- 不新增依赖，不扩展范围。

【委派】
父 Agent 只确认范围并检查 diff；子 Agent 定位、修改并做文档级自检。

【验收】
- 旧命令不再出现在目标段落。
- 其他段落无无关改动。

【回报】
只返回状态、改动文件和自检结果。
